# EBPF with Cilium and golang

### install
- Install fedora 36 as development environment
- Install dependencies to get the project running \
`sudo dnf install clang llvm go make libbpf-devel bpftool -y`